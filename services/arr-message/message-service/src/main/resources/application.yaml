spring:
  application:
    name: message-service
  profiles:
    include: feign,flyway,orm,redis,tomcat,rest
  flyway:
    table: message_schema_history
  main:
    allow-bean-definition-overriding: true
