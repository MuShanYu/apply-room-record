spring:
  flyway:
    enabled: true
    baseline-version: 1
    encoding: utf-8
    baseline-on-migrate: true
    # flyway 的 clean 命令会删除指定 schema 下的所有 table, 需要禁掉。
    clean-disabled: true
    # 是否允许不按顺序迁移
    out-of-order: true
    locations: classpath:db/migration